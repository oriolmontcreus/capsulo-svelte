# Nothing is required: `pnpm dev` uses a local D1 database and KV namespace.
#
# Local Worker variables go in `.dev.vars` (not this file), for example:
#   DEV_AUTO_LOGIN=false   # test the real login flow instead of the automatic dev sign-in
